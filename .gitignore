# Godot editor cache/import artifacts (regenerated per machine)
godot/.godot/

# Debug signing keystore -- not a real secret (well-known alias/password),
# but binaries don't belong in the repo. Regenerated identically by both
# local dev and CI via keytool; see export_presets.cfg.
godot/debug.keystore

# Local export output
godot/build/

# Stray APK export dir that landed at repo root from an earlier slice's build
UsersmrscpAppDataLocalTempapk-slice220/

# Generated production ZIP (regenerable from the source .md docs it bundles)
CRXCIBL3_ANIMATION_VOICEOVER_PRODUCTION_PACKAGE.zip
__pycache__/
*.pyc
.env
.cursor/gate-logs/
.cursor/gate-cache/
